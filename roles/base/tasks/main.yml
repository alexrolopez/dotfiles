- name: Install apt packages
  apt:
    name:
      - ansible
      - build-essential
      - curl
      - docker-compose
      - fzf
      - htop
      - jq
      - make
      - memcached
      - postgresql
      - redis
      - shellcheck
      - tldr
      - tree 
      - vim
      - wget
    state: latest
    update_cache: yes
  become: yes
