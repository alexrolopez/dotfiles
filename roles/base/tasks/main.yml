- name: Install apt packages
  apt:
    name:
      - ansible
      - build-essential
      - curl
      - fzf
      - htop
      - shellcheck
      - tldr
      - tree 
      - wget
    state: latest
    update_cache: yes
  become: yes
